extends layout.pug

block content
  section#content
    h2(title='question')=question
    if show_answer
      p
        strong(title='answer')=answer
      p
        a(href=`/card/${next}`) Show Next
    else
      p
        a(href=`/card/${number}?show_answer=true`) Show Answer
    p
      strong(title='card_number')='Card number: ' + number + ' of ' + length
